extends layout

block content
    .card.post
        .top
            img#header-image(src=post.image)
            .bar
                .title
                    h2= post.title
                .date= post.date
        p
            strong= post.description
        | !{post.body}

block bottom
    .card
        p You may also like